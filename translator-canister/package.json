{
  "name": "translator-canister",
  "version": "1.0.0",
  "description": "ICP canister to manage natural language queries to a backend API",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/translator-canister-frontend/* dist/",
    "clean": "rm -rf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "People and Code",
  "license": "ISC",
  "dependencies": {
    "@dfinity/agent": "^0.19.2",
    "@dfinity/candid": "^0.19.2",
    "@dfinity/principal": "^0.19.2"
  }
}
